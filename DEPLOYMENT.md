# Railway Deployment Checklist

## ✅ Pre-Deployment Checklist

- [x] Railway configuration files created (`railway.toml`, `nixpacks.toml`)
- [x] Environment variables configured for Railway
- [x] Database setup script ready (`setup-database.js`)
- [x] GitHub Actions workflow configured
- [x] README updated with deployment instructions
- [x] Package.json includes build script

## 🚀 Deployment Steps

### 1. Push to GitHub
```bash
git add .
git commit -m "Configure for Railway deployment"
git push origin main
```

### 2. Railway Setup
1. Go to [railway.app](https://railway.app)
2. Click "New Project" → "Deploy from GitHub repo"
3. Select this repository
4. Railway will auto-detect Node.js app

### 3. Environment Variables in Railway
Copy these variables to Railway dashboard:
```
PORT=5000
DB_HOST=mysql.railway.internal
DB_USER=root
DB_PASSWORD={auto-generated by Railway}
DB_NAME=railway
SESSION_SECRET={generate secure secret}
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=no-reply@example.com
EMAIL_PASSWORD={your email password}
RAZORPAY_KEY_ID={your Razorpay key}
RAZORPAY_KEY_SECRET={your Razorpay secret}
BASE_URL=https://your-app-name.railway.app
ADMIN_INVITE_CODE=globalbangla12
```

### 4. Deploy
- Railway will automatically deploy
- Your app will be live at `https://your-app-name.railway.app`

## 🎯 Post-Deployment

1. **Test the application**
   - Visit your Railway URL
   - Check if pages load correctly
   - Test user registration

2. **Create admin account**
   - Visit `/admin/`
   - Use invite code: `globalbangla12`
   - Create admin account

3. **Verify database**
   - Check if database tables were created
   - Test user functionality

## 🔧 Troubleshooting

### Common Issues:
- **Database connection failed**: Check Railway environment variables
- **Build fails**: Verify `package.json` scripts
- **500 errors**: Check Railway logs for detailed errors

### Railway Logs:
- Go to Railway dashboard
- Select your project
- Click "Logs" tab

## 📱 Access Your Live App

Once deployed, your app will be available at:
- **Main site**: `https://your-app-name.railway.app`
- **Admin panel**: `https://your-app-name.railway.app/admin`

## 🔄 Automatic Deployments

Railway will automatically redeploy when you:
- Push to main branch
- Update environment variables
- Modify configuration files
